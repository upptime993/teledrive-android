package com.teledrive.sky.ui.theme

import androidx.compose.ui.graphics.Color

// ─── TeleDrive Sky Color Palette ────────────────────────────────────────────
// Premium dark-blue theme, terinspirasi dari Telegram tapi lebih refined

// Primary Blues — Medium-deep blue, tidak terlalu bright
val SkyBlue = Color(0xFF2B7BE8)         // Primary action blue
val SkyBlueLight = Color(0xFF5B9FF0)    // Lighter variant
val SkyBlueDark = Color(0xFF1A5BBE)     // Darker pressed state
val SkyBlueContainer = Color(0xFF1C3461) // Container/card blue tint

// Accent
val SkyAccent = Color(0xFF4FC3F7)       // Light sky blue accent
val SkyAccentDim = Color(0xFF1A4A6E)    // Dimmed accent for containers

// Neutral Dark Palette — Deep navy, not pure black
val SkyBackground = Color(0xFF090E1D)   // Main background — deepest navy
val SkySurface = Color(0xFF101726)      // Cards & containers
val SkySurfaceElevated = Color(0xFF162035)  // Elevated surface (dialogs, sheets)
val SkySurfaceHighest = Color(0xFF1E2D47)   // Highest elevation (tooltips)

// Gradient colors
val SkyGradientStart = Color(0xFF0A1628)
val SkyGradientEnd = Color(0xFF0D1F3C)
val SkyCardGradientStart = Color(0xFF12213A)
val SkyCardGradientEnd = Color(0xFF0F1B2E)

// Text Colors
val SkyTextPrimary = Color(0xFFEBF4FF)      // Primary text — soft white
val SkyTextSecondary = Color(0xFF8BAFD4)     // Secondary text — muted blue-grey
val SkyTextTertiary = Color(0xFF4A6A8A)      // Tertiary — even more muted
val SkyTextOnBlue = Color(0xFFFFFFFF)        // Text on blue backgrounds

// Status Colors
val SkySuccess = Color(0xFF34C759)      // iOS-style green
val SkySuccessContainer = Color(0xFF0D2E18)
val SkyWarning = Color(0xFFFF9F0A)      // Warm amber
val SkyWarningContainer = Color(0xFF2E1F04)
val SkyError = Color(0xFFFF453A)        // Vibrant red
val SkyErrorContainer = Color(0xFF2E0A08)
val SkyInfo = Color(0xFF64D2FF)         // Info blue

// File Category Colors
val ColorPhoto = Color(0xFF4FC3F7)      // Sky blue for photos
val ColorVideo = Color(0xFFBA68C8)      // Purple for video
val ColorDocument = Color(0xFF66BB6A)   // Green for documents
val ColorAudio = Color(0xFFFF8A65)      // Orange for audio
val ColorArchive = Color(0xFFFFCA28)    // Amber for archives
val ColorOther = Color(0xFF90A4AE)      // Blue-grey for others

// Dividers & Borders
val SkyDivider = Color(0xFF1A2A3E)
val SkyBorder = Color(0xFF1E3050)
val SkyBorderFocused = Color(0xFF2B7BE8)

// Overlay
val SkyOverlay = Color(0x99090E1D)
val SkyScrim = Color(0xCC090E1D)

// Transfer Progress
val SkyProgressBg = Color(0xFF162035)
val SkyProgressFill = Color(0xFF2B7BE8)
val SkyProgressTrack = Color(0xFF1A2A3E)
